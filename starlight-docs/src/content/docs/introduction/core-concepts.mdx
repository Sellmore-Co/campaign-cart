---
title: Core Concepts
description: Key concepts for understanding Campaign Cart architecture and data flow
---

import { Steps, Aside, Card, CardGrid, Code, Tabs, TabItem } from '@astrojs/starlight/components';

Understanding these core concepts will help you make the most of Campaign Cart.

## Packages vs Profiles

Campaign Cart supports two ways to reference products:

<Tabs>
  <TabItem label="Product Profiles (Recommended)">
    **Semantic IDs** - Use meaningful names instead of numbers
    
    <Code code={`<button data-os-action="toggle-item" data-os-profile="starter-kit">
    Add Starter Kit
</button>`} lang="html" />

    **Benefits:**
    - Country-aware automatic mapping
    - Metadata support (categories, tags)
    - Better for multi-currency setups
    - More maintainable code
  </TabItem>
  
  <TabItem label="Packages (Legacy)">
    **Numeric IDs** - Direct package references
    
    <Code code={`<button data-os-action="toggle-item" data-os-package="1">
    Add Package #1
</button>`} lang="html" />

    **Limitations:**
    - Less flexible for international sites
    - Harder to maintain
    - No automatic currency mapping
  </TabItem>
</Tabs>

<Aside type="tip">
We strongly recommend using Product Profiles for new implementations, especially if you plan to support multiple currencies.
</Aside>

Learn more: [Product Profiles Guide](../guides/features/product-profiles)

## The Cart System

### Cart State
The cart maintains state across pages:
- Items (products/packages)
- Quantities
- Applied discounts
- Selected variants

### Cart Persistence
- Stored in browser's localStorage
- Synced with 29next API
- Survives page refreshes
- Cleared after successful purchase

### Cart Operations
```javascript
// Add item
window.twentyNineNext.addToCart(1);
window.twentyNineNext.profiles.addToCart('starter-kit');

// Remove item
window.twentyNineNext.removeFromCart(1);

// Clear cart
window.twentyNineNext.clearCart();

// Get cart contents
const items = window.twentyNineNext.getCart();
```

## Display Elements

Campaign Cart uses HTML data attributes to create interactive elements:

### Action Elements
- `data-os-action="toggle-item"` - Add/remove from cart
- `os-checkout-payment="combo"` - Proceed to checkout
- `data-os-component="selector"` - Product selection

### Display Elements
- `data-os-cart-count` - Number of items
- `data-os-cart-total` - Total price
- `data-os-package-price` - Product price
- `data-os-cart-summary="savings-amount"` - Discount amount

## Events System

Campaign Cart emits events throughout the customer journey:

### Cart Events
```javascript
document.addEventListener('cart.updated', (event) => {
    console.log('Cart updated:', event.detail);
});
```

### Purchase Events
```javascript
document.addEventListener('purchase.completed', (event) => {
    console.log('Purchase completed:', event.detail);
});
```

## Multi-Currency

Campaign Cart automatically handles multiple currencies:

<Steps>
1. **Detection** - Uses Cloudflare Workers to detect user location
2. **Campaign Switching** - Loads appropriate campaign for the country  
3. **Package Mapping** - Translates package IDs between campaigns
4. **Display** - Shows correct currency symbols and formatting
</Steps>

## Configuration Hierarchy

Configuration follows this priority order:

<Steps>
1. **JavaScript Config** (`window.osConfig`) - Highest priority
2. **Meta Tags** (`<meta name="os-*">`) - Page-level settings
3. **Data Attributes** (element-specific) - Component-level
4. **Defaults** (built-in fallbacks) - Lowest priority
</Steps>

<Aside type="note">
Higher priority settings override lower ones. Use JavaScript config for dynamic values and meta tags for static page settings.
</Aside>

## Checkout Flow

The checkout process follows these steps:

<Steps>
1. **Cart Review** - User reviews items and quantities
2. **Customer Info** - Email and name collection
3. **Billing Address** - Complete address details
4. **Payment Method** - Credit card, PayPal, Apple Pay, etc.
5. **Order Review** - Final confirmation before payment
6. **Processing** - Secure payment processing
7. **Receipt** - Order confirmation and details
</Steps>

## Typical Funnel Workflow

The primary goal of Campaign Cart is to let you quickly power a sales funnel on any website. Here is the typical step-by-step workflow from a developer's perspective.

```mermaid
graph TD
    A["Step 1: Get Product IDs<br/>Get Package IDs from your<br/>29next Campaign Admin"] --> B;
    B["Step 2: Build Sales Page<br/>Use HTML attributes to add products to the cart.<br/>- Use Selectors for primary choices<br/>- Use Toggles for add-ons"] --> C;
    C["Step 3: Build Checkout Page<br/>Set meta tags:<br/>- os-page-type='checkout'<br/>- os-next-page='/path/to/upsell'<br/>SDK handles form validation and payment processing."] --> D;
    D["Step 4: Build Upsell Page (Optional)<br/>Set meta tags:<br/>- os-page-type='upsell'<br/>- os-next-page='/path/to/receipt'<br/>Use data-os-upsell attributes for one-click offers."] --> E;
    E["Step 5: Build Receipt Page<br/>Set meta tag:<br/>- os-page-type='receipt'<br/>SDK automatically fetches and displays all order data."]
```

## Upsells

Post-purchase upsells extend the checkout:

1. **Main Purchase** - Initial order
2. **Upsell Offer** - Additional product offer
3. **Accept/Decline** - Customer choice
4. **Next Upsell** - Multiple upsells possible
5. **Final Receipt** - Combined order summary

## Best Practices

<CardGrid>
  <Card title="1. Use Product Profiles" icon="star">
    <Code code={`<!-- Recommended -->
<button data-os-action="toggle-item" data-os-profile="starter-kit">Add</button>

<!-- Legacy approach -->
<button data-os-action="toggle-item" data-os-package="1">Add</button>`} lang="html" />
  </Card>
  
  <Card title="2. Handle Events" icon="broadcast">
    <Code code={`document.addEventListener('cart.updated', updateUI);
document.addEventListener('purchase.completed', trackConversion);`} lang="javascript" />
  </Card>
  
  <Card title="3. Configure Properly" icon="setting">
    <Code code={`window.osConfig = {
    apiKey: 'YOUR_KEY',
    campaignId: 'YOUR_CAMPAIGN',
    defaultCountry: 'US',
    analytics: {
        gtm: true,
        fbPixel: 'YOUR_PIXEL_ID'
    }
};`} lang="javascript" />
  </Card>
  
  <Card title="4. Test Thoroughly" icon="test-tube">
    - Use `?test=true` for test mode
    - Test different countries with `?forceCountry=CA`
    - Verify analytics tracking  
    - Check mobile responsiveness
  </Card>
</CardGrid>

<Aside type="caution">
Always test your implementation thoroughly before going live. Use test mode to avoid processing real payments during development.
</Aside>

## Next Steps

- [Shopping Cart Guide](../guides/features/shopping-cart.md) - Deep dive into cart features
- [Product Profiles Guide](../guides/features/product-profiles.md) - Advanced product management
- [Events Reference](../api/events-reference.md) - Event handling and analytics
- [JavaScript API](../api/javascript-api.md) - Complete API documentation
- [Basic Configuration](../guides/configuration/basic-config.md) - Configuration options